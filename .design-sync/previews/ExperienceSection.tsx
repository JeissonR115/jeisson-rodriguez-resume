import { ExperienceSection } from 'jeisson-rodriguez-resume';

// ExperienceSection takes no props — real work history from
// src/data/profile.json and i18next's "experience" namespace.
export function Default() {
    return <ExperienceSection />;
}
