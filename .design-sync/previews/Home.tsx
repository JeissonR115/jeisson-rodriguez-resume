import { Home } from 'jeisson-rodriguez-resume';

// Home (the hero section) takes no props — real profile data + real
// i18next copy, router-aware CTA buttons via the shared MemoryRouter.
export function Default() {
    return <Home />;
}
