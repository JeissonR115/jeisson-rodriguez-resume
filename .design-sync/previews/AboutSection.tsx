import { AboutSection } from 'jeisson-rodriguez-resume';

// AboutSection takes no props — real profile summary + highlights from
// src/data/profile.json and i18next's "about" namespace.
export function Default() {
    return <AboutSection />;
}
