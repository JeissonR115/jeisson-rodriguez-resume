import { SkillsSection } from 'jeisson-rodriguez-resume';

// SkillsSection takes no props — real technical/soft skill scores from
// src/data/skills.json and i18next's "skills" namespace.
export function Default() {
    return <SkillsSection />;
}
